<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Dashboard</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="list-container">
        <h2>Bem-vindo, {{name}}</h2>
        <p><a href="/history">Ver histórico de compras</a></p>
        
        <h3>Ingressos Disponíveis</h3>
        <ul>
            {{#tickets}}
            <li>
                {{name}} - R$ {{price}} - {{quantity}} disponíveis
                <form action="/purchase" method="POST" class="purchase-form">
                    <input type="hidden" name="ticketId" value="{{_id}}">
                    <input type="number" name="quantity" min="1" max="{{quantity}}" required>
                    <button type="submit">Comprar</button>
                </form>
            </li>
            {{/tickets}}
        </ul>

        <form action="/logout" method="POST">
            <button class="logout-btn" type="submit">Sair</button>
        </form>
    </div>
</body>
</html>
